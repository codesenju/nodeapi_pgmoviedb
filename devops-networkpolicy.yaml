apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: devops
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/devops: "true"
  podSelector:
    matchLabels:
      io.kompose.network/devops: "true"
